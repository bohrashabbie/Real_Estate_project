# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
Api/.env
Api/uploads/

# Node
node_modules/
.next/
*.tsbuildinfo
admin/.env.local
storefront/.env.local

# misc
.DS_Store
*.log
.env

# Source images already uploaded via the admin API (live in the uploads volume)
media/

# This repo is public on GitHub — credential dumps dropped into reference/
# must never be pushed. Keep them local.
reference/*Credentials*
